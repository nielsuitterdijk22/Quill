Repository

- branch button should have a dropdown on clicking, not a link to branches
- PR merge button to the top right /w popup for merge type
- PR approval flow is not great
- PR code comments don't appear on the PR page, only in the code view
- Approving / denying own PRs gives a forgejo error
- cards on settings need padding
- http://localhost:3001/orgs/acme/repos/api-gateway vs. http://localhost:3001/orgs/acme/repos/api-gateway/tree/main > latter doesn't render README.md, every page should render a readme
- Readme render is not looking good
- pipelines lists repos, not pipelines (while showing source repo)
- first org / project user should be owner.
- access tokens are not stored -- cannot be revoked
- Across quill / tempo, we need to settle on vocabulary. Org > Team > Project is not great - cannot picture having multiple orgs in a single company.
